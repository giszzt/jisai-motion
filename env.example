# Copy to .env in this folder and fill in the key. Never commit .env.
# One key serves every image skill; ~/.claude/image-backends.env works too.
LABNANA_API_KEY=
# Optional. Defaults to https://api.labnana.com
# LABNANA_BASE_URL=
